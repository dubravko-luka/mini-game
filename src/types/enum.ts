export { }

export enum keysQuery {
}

export enum KEYCODE_STRING {
  SPACE = 32,
  VIRGULE = 191
}

export enum TYPE_ENUM {
  DEFAULT = 'DEFAULT',
  NOT_INPUT = 'NOT_INPUT',
  VALID = 'VALID',
  INVALID = 'INVALID',
}